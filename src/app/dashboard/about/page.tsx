import DashboardSettingsView from 'src/sections/_dashboard/view/dashboard-settings-view';

// ----------------------------------------------------------------------

export const metadata = {
  title: 'Settings',
};

export default function DashboardSettingsPage() {
  return <DashboardSettingsView />;
}
